#include <stdlib.h>
#include <stdio.h>
#include <string.h>


void
normalize_path(char *buf) {
    int len = strlen(buf);
    for(int i = 0; i < n; i++) {

    }
}


int main(void) {
    char a[100];
    scanf("%s", a);
    normalize_path(a);
    printf("%d", a);
    return 0;
}